<%
# For linked zones, use the child zone's key but the parent zone's definition
if zone_name != name
%>
/* This zone is linked from <%= name.to_json %> */
<% end %>
module.exports = {
  <%= (zone_name || name).to_json %> : <%= data.to_json %>
};
